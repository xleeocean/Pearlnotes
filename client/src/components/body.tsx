import * as React from 'react';
import Navigator from './navigator';
import MainBox from './mainbox';

function Body() {
  return (
    <div>
      <Navigator/>
      <MainBox />
    </div>
  );
}

export default Body;
